# Optional config
VA_LOCALE=en_ZA
VA_NOTES_FILE=notes.txt

# Voice settings
VA_MODEL_PATH=./models/vosk-model-small-en-us-0.15   # path to Vosk model folder
VA_STT_SECONDS=4                                     # seconds to listen per prompt (fallback)
VA_INPUT_DEVICE=                                     # optional: sounddevice input device index
VA_TTS_RATE=200                                      # pyttsx3 words per minute
VA_TTS_VOICE=                                        # optional voice id/name (leave blank to default)

# APIs
VA_OPENWEATHER_KEY=YOUR_OPENWEATHER_API_KEY
VA_WIKI_LANG=en
